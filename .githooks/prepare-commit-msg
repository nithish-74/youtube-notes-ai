#!/bin/sh
# Strip Cursor co-author lines so GitHub only credits you.
if [ -f "$1" ]; then
  grep -v 'cursoragent@cursor.com' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
  grep -vi 'Co-authored-by: Cursor' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
